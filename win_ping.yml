---
- hosts: win
  tasks:
    - name: My first winrm ping
      win_ping:

    - name: My second winrm ping
      shell:
          executable: pwd
      register: foo
      tag: secondping

    - debug: msg=foo
