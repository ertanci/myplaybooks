---
- hosts: c1
  tasks:

   - name: Execute the command in remote shell
     ansible.builtin.shell: bScript.sh
     register: myhostname

   - name: write my pass variable
     debug: 
        msg: "myhostname is {{myhostname.stdout}}"
